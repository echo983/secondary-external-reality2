{
  "name": "secondary-external-reality2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval:qwen:runtime-smoke": "npm run build && node dist/scripts/qwen-runtime-smoke.js",
    "eval:qwen:smoke": "node experiments/qwen-boundary/run.mjs --smoke",
    "eval:qwen:full": "node experiments/qwen-boundary/run.mjs",
    "eval:qwen:score": "node experiments/qwen-boundary/score.mjs"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.6"
  }
}
